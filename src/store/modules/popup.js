const state = {
  counter: 5,
};

const getters = {
  counter(state) {
    return state.counter;
  },
};

const actions = {};

const mutations = {};

export default {
  state,
  getters,
  actions,
  mutations,
};
