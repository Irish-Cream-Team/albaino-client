const state = {
  // counter: 0,
  // newProjectPopup,
};

const getters = {
  // counter: (state) => state.counter,
};

const actions = {
  // increment(context) {
  //   context.commit("increment");
  // },
};

const mutations = {
  // increment: (state) => state.counter++,
};

export default {
  state,
  getters,
  actions,
  mutations,
};
